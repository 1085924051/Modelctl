# 模型适配器协议 v0 草案

状态：设计草案。此协议描述控制面与模型适配器之间的边界，供 Laya 和第二类开放模型共同验证；字段和错误码尚未实现。产品范围见 [决策清单](PRODUCT_DECISIONS.md)。

## 设计原则

控制面负责目录、工件、任务、实例和访问控制；适配器负责特定运行时的预检、加载和调用。一个适配器可以支持多个模型定义，一个模型定义也可以有多个运行时 profile。对外的操作名由能力声明给出，不预设所有模型都是聊天模型。权重和执行代码分别安装、分别标识版本。

首版内置适配器由工具发行包提供。第三方适配器将来按同一协议运行在独立进程中，须显式安装与启用；模型清单不能自行指定任意可执行路径。为避免过早绑定一种语言或引擎，控制面只依赖版本化消息与 HTTP 端点，不加载适配器代码到自身进程。

## 对象标识

| 名称 | 含义 | 示例 |
| --- | --- | --- |
| `model_id` | 发布者与模型名 | `convaiinnovations/laya` |
| `model_revision` | 固定的模型仓库提交或发行版本 | Hub commit SHA |
| `variant` | 同一模型的 checkpoint 或量化变体 | `english`、`multilingual` |
| `runtime_id` | 适配器实现及其版本 | `laya-python@0.1` |
| `profile_id` | 设备和启动参数组合 | `cpu`、`cuda`、`mps`、`auto` |
| `instance_id` | 一次进程启动的唯一标识 | UUID |

公共 HTTP 路径中的模型标识必须作为完整路径参数转义，或者改用查询参数及 JSON 请求体；不能让 `publisher/name` 的斜杠改变路由层级。实例标识始终是单段路径参数。

## 能力声明

适配器在安装时提供静态能力目录，在实例启动后报告实际可用能力。每条能力至少包含：

```json
{
  "name": "system_one",
  "kind": "structured_decision",
  "input_schema": "schema://laya/system-one-input/v1",
  "output_schema": "schema://laya/system-one-output/v1",
  "streaming": false,
  "media": ["application/json"],
  "max_concurrency": 1
}
```

Schema URI 只是草案写法，正式版要么随适配器提供内容及哈希，要么在能力查询 API 内嵌 schema；客户端应能在离线状态验证输入。通用字段只描述可发现性，不改写模型原有请求与响应。Laya 的 `system_one` 保留 `state`、`questions` 和概率输出；文本生成模型可另声明 `generate_text`，并定义流式事件。MCP 工具与 Skill 所引用的是这些能力名及 schema。

`media` 表示能力接受的请求内容类型，不意味着请求总是 JSON。媒体能力还需声明最大请求字节数、可接受格式、是否需要预处理、是否支持分段/流式输入与输出。公共 API 根据能力声明验证请求，再将字节流或受管理的文件句柄交给适配器。客户端提供的任意绝对文件路径不得直接传给模型进程；若支持文件输入，控制面先在允许目录内打开并验证文件，或接收上传到受管理的临时目录。临时文件按请求生命周期删除，大小和磁盘配额单独限制。

## 生命周期协议

控制面和适配器应完成以下消息交换：

| 操作 | 输入 | 输出/行为 |
| --- | --- | --- |
| `hello` | 控制面支持的协议版本 | 协商后的版本、适配器标识和能力目录 |
| `probe` | 主机信息、模型定义、profile | 支持程度、缺失依赖、预计资源和设备候选 |
| `prepare` | 已校验的工件库、运行时配置 | 构建可启动的模型目录与环境；不再隐式下载权重 |
| `start` | 实例 ID、已准备的模型目录、设备、内部凭据 | 受管理进程句柄、仅本机可达的端点、实际设备 |
| `health` | 实例 ID | `starting/ready/stopping/failed`、已加载 variant 与原因 |
| `invoke` | 实例 ID、能力名、请求、超时和取消标识 | 原生结果或版本化流式事件 |
| `stop` | 实例 ID、宽限时间 | 停止接单、清理进程、释放资源 |

每个启动请求包含不可重用的 `instance_id`；同一 ID 的重试必须返回既有实例状态或明确失败，不能重复占用显存。`prepare` 可重复执行，已完成的环境应可复用。控制面重启后，以实际进程与探活结果重建状态；若旧进程无法证明归属，应标为待处理，不能盲目把数据库记录当成运行事实。

工件库保存按哈希校验的原始文件，不交给适配器修改。`prepare` 在运行时目录中组装模型文件；对需修改的配置文件使用私有副本，其他大文件可用只读硬链接或复制，但必须保证上游不会原地修改它们。每个运行时目录的来源、生成规则和生成文件哈希都要记录。多个实例可以共享已准备且不会再改动的目录；需要写入缓存或修改配置的实例应获得独立可写目录。Laya 的 tokenizer 配置是已知例子，上游加载时可能修补 `tokenizer_config.json`，因此不能直接把只读的原始快照路径传给它。适配器升级或原始文件变更时重新执行 `prepare`，避免旧目录与新包版本混用。

适配器子进程的控制消息可以先走本机 HTTP/Unix domain socket；模型推理可直接代理其原生 HTTP 路由。消息必须有协议版本、请求 ID、截止时间和结构化错误。标准输出/错误仅用于日志，避免混入控制消息。若某平台上的 socket 支持或进程隔离不同，可替换传输而保持字段语义。内部端点随机端口、回环绑定，并用每实例凭据防止本机其他进程直接调用；正式版还应说明本机多用户权限边界。

## 错误与取消

至少区分 `UNSUPPORTED_PLATFORM`、`DEVICE_UNAVAILABLE`、`INSUFFICIENT_MEMORY`、`ARTIFACT_MISSING`、`ARTIFACT_CORRUPT`、`RUNTIME_INCOMPATIBLE`、`MODEL_LOAD_FAILED`、`INVALID_REQUEST`、`DEADLINE_EXCEEDED`、`CANCELLED`、`WORKER_EXITED`。错误返回机器可读代码、简短说明和可选的修复建议；日志保留更详细的内部诊断。取消请求应尽力中止推理，不能中断时仍要释放等待队列与客户端连接，并明确反馈实例是否仍在执行。

## 不同能力的设计检验

| 维度 | Laya：`system_one` | Whisper：`transcribe_audio` | 文本生成：`generate_text` |
| --- | --- | --- | --- |
| 输入 | 状态与多个 `choice/score/noul` 问题 | 音频字节或受管理文件句柄、语言/转写参数 | 提示文本与生成参数 |
| 输出 | 各问题答案、概率、路由与用量 | 文本片段、时间戳、语言信息 | 文本片段、结束原因与用量 |
| 典型执行 | 一次前向计算，非流式 | 文件上传、解码/转写，可长时间运行 | 逐 token 流式，可取消 |
| 资源 | 权重、tokenizer、CPU/CUDA/MPS | GGML 权重、音频临时空间、CPU/Metal/CUDA | 权重、tokenizer、KV cache、CPU/GPU |
| 兼容 API | 可提供 Jev `/v1/systemone` | 按适配器 schema 提供转写操作 | 只有明确支持时才提供聊天或生成兼容 API |

Whisper 候选采用 [whisper.cpp](https://github.com/ggml-org/whisper.cpp) 的原生 C/C++ 运行时。其上游提供 Linux/macOS 构建、模型下载说明和 `whisper-server`，后者接受 multipart 音频上传。它与 Laya 的运行环境和输入媒介均不同，适合检验第二个适配器。协议共同承担安装、预检、启动、健康、调用和停止，但能力 schema、媒体传输、流式模式和资源计算由适配器声明。此表仍是设计检验；接入和实机运行尚未完成。第二个真实适配器接入时，如必须修改公共生命周期 API，应先修订此协议再宣布稳定。

## MCP 与 Skill 的位置

MCP 代理从控制面读取实例与能力目录，把允许公开的能力映射为 tool。它调用现有实例的 `invoke`，不重新装载权重。一个能力可以提供不同的 MCP 呈现，但转换规则要显式配置，不能因 `kind` 相似就自动暴露文件或网络权限。

Skill 包声明所需能力名、schema 版本和可选的外部权限；执行器通过公共 API 调用。Skill 不是模型包，也不能成为适配器安装脚本。即使未来支持插件市场，三种包仍应各自有来源、版本、授权和更新路径。

## 开发前需锁定

1. 核定 `whisper.cpp` 及一个固定的 Whisper GGML 模型版本、权重来源与许可证，使用上游 `whisper-server` 的上传调用样例检验音频能力；若产品更重视文本生成，再另选生成模型。
2. 确定 Linux/macOS 的必需硬件组合、Python 管理方式及桌面界面是否进入首版。
3. 为 Laya 固定包版本、依赖锁文件、Hub commit 和全部工件哈希，并在目标机器上测量资源需求。
4. 冻结公共 API 的路由形式、schema 发布方式以及流式事件格式。
