# 产品决策清单

状态：供讨论。已确认的要求与建议默认值分开记录；未得到用户确认的建议不视为最终决定。技术细节见 [总体架构](ARCHITECTURE.md) 和 [Laya 适配器设计](LAYA_ADAPTER.md)。

## 已确认

- 目标是做一个类似 Ollama 使用体验、可部署多类开放模型的本地工具，预留 MCP 和 Skill 扩展接口。
- 首版目标操作系统：Linux 和 macOS。
- 首个具体模型：上游 [Laya](https://github.com/NandhaKishorM/laya)。[Ollama](https://github.com/ollama/ollama) 用作安装、命令和 API 体验参考。
- Laya 是结构化决策模型；它的 `system_one` 输入输出与聊天生成不同。Laya 上游已有本地服务和 MCP 实现，工具可复用其推理代码。

## 建议默认值

| 决策 | 建议 | 原因与取舍 |
| --- | --- | --- |
| 首个使用场景 | 个人开发者在单机通过 CLI 安装模型，其他应用通过本地 API 调用 | 与 Ollama 的核心路径接近，也为后续桌面界面留下同一个控制 API |
| 界面 | 首版交付 CLI 和 API；桌面界面随后接入 | 先验证模型生命周期与协议；界面可以直接复用控制 API |
| 支持的设备 | Linux CPU、macOS CPU 为共同基线；Linux NVIDIA CUDA 和 macOS Apple MPS 列为首版目标，但以实机验证为准 | 保证两平台存在共同运行路径，并覆盖常见加速设备 |
| 依赖安装 | 正式发行包管理独立 Python 运行时与适配器环境；开发预览版可以暂时要求用户安装 Python 3.10+ | 单命令部署体验不能依赖用户手动维护每个模型的环境；GPU 驱动仍需宿主提供 |
| 默认网络范围 | 仅本机回环地址 | 本地工具的最小配置；局域网共享作为显式模式设计 |
| 第二个模型 | 候选为 Whisper + `whisper.cpp`，提供音频转写 | 原生 C/C++ 运行时与文件上传路径能检验跨框架、跨媒介的接口；具体权重版本仍需确定 |
| Jev | 首版支持 Jev 兼容 API；暂不把官方 Jev 列为本地可安装模型 | 尚未证实官方权重可供本地部署；协议兼容与部署模型本体是不同承诺 |

## 首版的完成边界

在 Linux 和 macOS 上，从确定版本的清单安装 Laya，完成设备预检、启动、`system_one` 调用、状态和日志查看、停止与故障清理；同一实例可经本地 API 调用。安装过程应校验工件并保留许可证与来源。接口为 MCP/Skill 与其他适配器保留稳定入口。第二个真实模型、MCP 代理和 Skill 执行器作为紧接的后续里程碑，其交付结果另行验收。

## 需要你决定的三件事

1. **用户入口**：首版是 CLI/API 优先，还是要求桌面界面同步交付？
2. **硬件范围**：Linux 的 NVIDIA GPU 与 macOS 的 Apple Silicon/MPS 是否都必须进入首版验收？有无 AMD GPU、Intel Mac 或 Linux ARM64 的明确需求？
3. **安装体验**：首个公开版本是否必须做到无需用户预装 Python 的单命令安装？

若暂时不决定，这三项按上表建议推进设计，所有选择保持可调整。开始实现前，需要把“必须支持的硬件”和“公开版本安装体验”定为明确的验收条件。

## 当前推进假设

在收到明确取舍前，设计和原型按以下假设继续：

- 首版交付 CLI + 本地 API，桌面界面调用同一 API，暂不把桌面打包列为首版阻塞项。
- Linux 与 macOS 先验证 CPU 共同基线；CUDA 和 Apple MPS 保留适配器入口与预检，但未有实机证据前不宣称已支持。
- 开发预览版允许用户提供 Python 3.10+；正式发行的单命令安装再决定是否内置/自动获取 Python 运行时。
- 首版只实现 Laya 适配器；Whisper + `whisper.cpp` 作为第二阶段的跨运行时验证，不将其伪装成已经交付的功能。
